#include "event.h"

void event(SDL_Surface *screen, MDE_Polyhedron *poly){
	/*SDL_Event event;
	int stop=0;

	drawPolyhedron(screen,*poly);
	while(!stop){
		SDL_WaitEvent(&event);
		switch(event.type){
			case SDL_QUIT:
				stop=1;
			break;

			case SDL_KEYDOWN:
				switch(event.key.keysym.sym){
					case SDLK_ESCAPE:
						stop=1;
					break;
				}
			break;
		}
	}*/
}
